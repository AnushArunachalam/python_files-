# create a function that takes 2 parameters
# 2 parameters should be a list
# remove duplicates from both the list
# return a new list contains only unique values
# combine both the list, return a new list containing unique elements.

# def zotac(q, w):
#     combined = q + w
#     new_list = [10, 20, 30]
#     combined.extend(new_list)
#     return combined
# print(zotac(["a", "m", "a", "z", "i", "n", "g"], ["e", "n", "v", "i", "r", "o", "n", "m", "e", "n", "t"]))  


# create a function that takes 3 parameters
# 1st list, 2nd dict, 3rd string
# grab the first element from the list, grab only the dict values, from string > 1st elements, 4th element
# use zip function to zip list and dictionary values together and return a new list.
# return strings 1st and 4th element

def project(a, b, c):
